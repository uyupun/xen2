import 'package:flutter/material.dart';

class ZazenInProgress extends StatelessWidget {
  const ZazenInProgress({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
